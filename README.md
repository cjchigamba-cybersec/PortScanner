# PortScanner
Multithreaded Python port scanner built with socket and ThreadPoolExecutor. Scans a custom port range against any target host, attempts HTTP banner grabbing on open ports, and exports results to a structured JSON file. No external dependencies ,standard library only.
